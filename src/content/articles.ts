import type { Article } from "@/types/content";

/**
 * Travel Notes articles.
 *
 * CONTENT SAFETY: three approved articles have not been supplied. We do not fabricate
 * titles, summaries, or dates (Charter §18). Until real articles exist this stays empty,
 * and the Travel Notes section renders a neutral invitation to the Tyler Takes Off
 * publication — never placeholder cards. See docs/decisions/missing-inputs.md #2.
 */
export const featuredArticles: Article[] = [];
